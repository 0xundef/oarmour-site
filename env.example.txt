# Database (Required)
# Connect to Supabase, Neon, or any PostgreSQL database
DATABASE_URL="postgresql://user:password@host:5432/db?schema=public"

# Next Auth (Required)
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL="http://localhost:3000" # Set to your production URL in Vercel (e.g. https://yourapp.vercel.app)

# OAuth Providers
# Google (Required for Google Login)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Github (Optional - if used)
GITHUB_ID=
GITHUB_SECRET=

# Stripe (Required for Billing)
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=

# Analytics (Optional)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=

# UploadThing (Optional - if used)
UPLOADTHING_SECRET=
UPLOADTHING_APP_ID=
