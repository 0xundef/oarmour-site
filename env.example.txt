# Database (Required)
# Connect to Supabase, Neon, or any PostgreSQL database
DATABASE_URL="postgresql://user:password@host:5432/db?schema=public"

# Next Auth (Required)
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL="http://localhost:3000" # Set to your production URL in Vercel (e.g. https://yourapp.vercel.app)

# OAuth Providers
# Google (Required for Google Login)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Github (Optional - if used)
GITHUB_ID=
GITHUB_SECRET=

# Stripe (Required for Billing)
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=

# Analytics (Optional)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=

# UploadThing (Optional - if used)
UPLOADTHING_SECRET=
UPLOADTHING_APP_ID=

# Convention: for any var below, the literal value `default` (or empty/absent) means
# "use the built-in default". GitHub forbids empty var values, so set `default` when
# you don't want an override. Secrets (API_KEY) must be real keys, never `default`.

# Investigation chat (Anthropic-compatible). DeepSeek's Anthropic endpoint by default.
# Local test: ANTHROPIC_CHATBOX_API_KEY=<deepseek key>
ANTHROPIC_CHATBOX_API_KEY=
ANTHROPIC_CHATBOX_BASE_URL=default
# Comma-separated allowlist for the chat model picker (default = deepseek-v4-flash).
ANTHROPIC_CHATBOX_MODEL=default
# disabled|enabled|adaptive (default = adaptive: enable for reasoner/v4-pro models)
ANTHROPIC_CHATBOX_THINKING=default

# Detection Pipeline (agentic recon/find/dedupe/report audit). Claude Agent SDK.
# Same DeepSeek Anthropic endpoint as the chat by default.
# Local test: ANTHROPIC_API_KEY=<deepseek key>
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=default
# Per-stage models (precedence: DETECTION_<STAGE>_MODEL > DETECTION_PIPELINE_MODEL > per-stage default).
# Defaults: recon=deepseek-v4-flash, find=deepseek-v4-pro, dedupe=deepseek-v4-pro, report=deepseek-v4-flash.
# Set DETECTION_PIPELINE_MODEL to force one model for all stages.
DETECTION_PIPELINE_MODEL=default
DETECTION_RECON_MODEL=default
DETECTION_FIND_MODEL=default
DETECTION_DEDUPE_MODEL=default
DETECTION_REPORT_MODEL=default
# Parallel find agents over partitions (default = 3)
DETECTION_FIND_CONCURRENCY=default
# Override the threat-model skill directory (default = lib/detection-pipeline/skills/threat-model)
DETECTION_THREAT_MODEL_DIR=default

